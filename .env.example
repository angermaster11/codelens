# Backend API URL - set this in Vercel environment variables
# For local dev, leave empty (Vite proxy handles it)
# For production, set to your deployed backend URL e.g. https://your-backend.onrender.com/api
VITE_API_URL=
